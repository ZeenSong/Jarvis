apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jarvis
resources:
  - namespace.yaml
  - postgres.yaml
  - server.yaml
  - network-policy.yaml
configMapGenerator:
  - name: jarvis-prices
    files:
      - prices.json
