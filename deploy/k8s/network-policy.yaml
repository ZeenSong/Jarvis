apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgres-private
spec:
  podSelector:
    matchLabels:
      app: postgres
  policyTypes: [Ingress]
  ingress:
    # hostNetwork traffic is node-origin traffic; enforcement depends on CNI.
    - from:
        - podSelector:
            matchLabels:
              app: jarvis-server
      ports:
        - protocol: TCP
          port: 5432
